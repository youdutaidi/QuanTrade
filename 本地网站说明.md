# Q-Forge 本地网站

## 最短启动方式

双击项目根目录中的 `启动本地网站.command`。终端窗口会保持开启，并自动打开：

<http://localhost:3000/>

关闭终端窗口或按 `Control-C` 即可停止网站。网站、页面数据和回测代码都保存在本机。

## 命令行启动

```bash
cd "/Users/cailingling/Desktop/quant-intelligence-lab"
npm install
npm run dev
```

## 更新因子回测后刷新网站

```bash
.venv/bin/qforge run --config configs/price_factors.json
```

该命令会更新 `app/data/factor_backtest.json`。开发服务器会自动刷新页面，无需重新部署。
