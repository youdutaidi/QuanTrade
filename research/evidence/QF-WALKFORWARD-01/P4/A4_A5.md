# P4 A4 computation and A5 report-state counterexample

A4 under `618650142557303bec2ec14fd97df4a02cb4ad26` completed the full
development calculation: 5,820,982 loaded rows, 5341 ordinary stocks, 1212
sessions from 2020-08-25 through 2025-08-22, all 16 frozen settings covering all
144 candidates. Each setting has finite eligible observations and no infinities.
No holdout rows, portfolio outcomes, winner or economic cash flows were computed.

Runtime 20.73 seconds externally; internal phases load 1.17s, align 10.57s,
score computation 8.42s. Peak RSS 3,177,234,432 bytes, zero swaps; the separate
macOS peak-footprint statistic was 5,634,840,096 bytes.

The actual JSON exposed a report-construction bug: spreading the upstream input
evidence after the workflow `state` replaced `development-features-checked`
with `verified-input`. The original A4 JSON/log remain unchanged. The existing
fixture lacked the upstream `state`, so it did not catch this collision.

A5 strengthened that retained fixture with the actual input-state field and
an explicit output-state assertion. It fails on the old merge order, as preserved
in `report-state-counterexample-A5.log`. Correct only the merge order, rerun all
retained tests and structure, and use a fresh output for the real recheck.
No factor equation, candidate, period, stock, price, cost or source artifact
changes with this reporting correction.
