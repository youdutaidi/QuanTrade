# P4 — real development signal input and cost preflight

Baseline `64f3bbf`. This is engineering preparation for the frozen study, not
a strategy-outcome experiment. QF-DATA-EXPANSION-01 remains the only active
empirical experiment while its raw action capture continues.

The A10 daily panel is admitted and fingerprinted. Load only discovery and
development rows through 2025-08-24 using the retained predicate-pushdown loader;
holdout rows must not enter the DataFrame. Read calendar/lifecycle references in
one read transaction, align the entire frozen ordinary A-share universe, and
compute the existing 16 shared score settings for the 144 unchanged candidates.

Do not choose a smaller universe, tune a parameter, select a winner, create a
portfolio, infer corporate-action cash flows or report returns. Verify axes,
absence of infinite values, finite observations for every setting and no finite
scores outside eligibility. Record load/alignment/feature timing and process
peak RSS. No large new score archive is necessary: the frozen input and code can
recompute it; retain the compact evidence summary.

Before a real attempt, preserve all 186 tests, add workflow and reference-date
fixtures, and pass the structure gate. Missing bars/reference sessions stop
before feature computation. If actual inputs fail, preserve the failure and
diagnose the narrow cause; no silent row dropping or scope reduction. If cost is
too high, review an equivalent implementation separately without changing the
frozen research question or accepting unverified economic P&L.
