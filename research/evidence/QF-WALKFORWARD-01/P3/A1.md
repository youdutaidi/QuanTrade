# P3 first synthetic gate — reference check timing counterexample

`pytest -q tests/test_walkforward_replay.py` initially returned 9 passed, 1 failed.
In `test_missing_distribution_preserves_failure_evidence_without_metrics`, the
missing distribution stopped the replay, but the final partial event was an
`order` rather than `session_open`: another symbol could be processed before
the affected symbol's reference-price guard fired.

No market outcome was read. No strategy or candidate sweep is admitted by this
failed gate. Correct the narrow account boundary: validate every held security's
opening reference after booking all distributions and before any opening order.
Keep the existing per-order and closing guards. Retain the counterexample test
and rerun the full suite before execution admission.

The subsequent full run had 165 passes and one stale test expectation: after
moving input validation before planning, no current-session intent should exist
when that validation fails. The corrected assertion requires all retained intents
to predate the failed session. A separate corrupted-quote case now verifies that
an error after planning does retain the actual requested orders. This does not
permit missing-distribution runs to return performance metrics.
