# A11 — lossless text normalization correction

Baseline `363101d`. This is an append-only continuation of the active data
experiment, not a new strategy study. Scope is frozen in the canonical research
README. Real source counterexample and original derived bytes remain in A10
and in the local SQLite archive.

Correct `dividCashStock` to text, retain the after-tax source text alongside
any unambiguous decimal projection, and flag the observed alternative-value
expression as ambiguous without choosing a branch. Version the new projection.
Provide a read-only re-normalization method that first verifies the archived
raw SHA256. Do not update legacy projections in place or repeat successful
source requests merely to change their representation.

Admission: retained tests, structure gate, then read-only re-normalization of
the single real response with before/after byte equality. Only afterward run
nine additional frozen code/year requests. Inspect every pilot response and
actual source cost before a larger source launch. No return search, holdout
inspection, tax decision, zero-defaulting or live order routing.
